# Model Artifacts

The original trained model is not published because it was trained using restricted project data.

This repository demonstrates the full machine-learning workflow using public-safe synthetic data, including:

- feature preprocessing
- anomaly detection
- threshold-based classification
- evaluation
- inference

Users can train their own model using the provided example pipeline.
